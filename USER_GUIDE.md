# OpenType 使用说明书

适用版本：当前 macOS 开发版

OpenType 是一个运行在 macOS 菜单栏里的 AI 语音输入工具。它的核心不是把声音机械地转成文字，而是根据你正在做的事情，把口语直接变成可以使用的最终文本。

## 1. 一分钟快速开始

第一次使用时，先完成四项准备：

1. AI 服务：在设置中为语音识别和文字生成各选择一个服务，并添加对应 Token。
2. 麦克风：允许 OpenType 录制语音。
3. 辅助功能：允许 OpenType 读取选中文字、监听全局快捷键并把结果写回输入框。
4. 实时字幕：允许 macOS 语音识别，在录音浮层里显示你正在说的话。

完成后，在任何输入框里：

1. 选择一个模式。
2. 长按左 `Option` 开始说话，松开后完成。
3. 等待 OpenType 识别和处理。
4. 结果会自动写入当前输入框。

无论自动写入成功还是失败，最终结果都会保留在剪贴板。按 `Command + V` 可以随时再次粘贴。

## 2. 快捷键怎么用

### 长按左 Option：适合短输入

使用情景：微信回复、一两句话、短邮件、搜索词、临时想法。

操作方式：

1. 把光标放到目标输入框。
2. 长按左 `Option`。
3. 说话。
4. 松开左 `Option`。

松开后，OpenType 会立即开始识别、整理并写入。

### 双击左 Option：适合连续录音

使用情景：较长的想法、文章段落、会议笔记、需要边想边说的内容。

操作方式：

1. 连续轻按两次左 `Option`。
2. 松开手，继续自然说话。
3. 按任意普通键结束录音。

用于结束的那个按键会被 OpenType 拦截，不会被输入到文本框里。

### 左 Option + Shift：切换模式

按下 `左 Option + Shift`，OpenType 会依次循环切换：

`智能编辑 → 中转英 → Agent 模式 → X Reply → 文字转写 → 智能编辑`

每次切换时，屏幕上会显示“已切换模式”和当前模式名称。

### 为什么有时显示 Control + Shift + Space

单独使用左 `Option` 需要 macOS 的辅助功能权限。在权限尚未生效时，OpenType 会临时回退到 `Control + Shift + Space`，以“按住说话、松开完成”的方式保证仍然可以使用。

授权完成后，OpenType 会自动恢复左 `Option`。

## 3. 五种模式分别解决什么问题

| 模式 | 最适合的情景 | OpenType 会做什么 | 使用前提 |
| --- | --- | --- | --- |
| 智能编辑 | 口述新内容，或修改已经写好的文字 | 没有选区时自动整理口述；有选区时按照明确的语音指令修改原文 | 口述时放好光标；修改时先选中文字 |
| 中转英 | 用中文思考，但需要自然英文输出 | 理解中文或中英混合口述，生成符合当前场景的地道英文 | 光标位于输入框 |
| Agent 模式 | 想直接完成任务，并延续最近几次任务的上下文 | 把语音目标变成可用成品，并读取本机结构化 Agent 记忆 | 不需要选中文字 |
| X Reply | 回复 X/Twitter 上的一条推文 | 读取原推文和你的中文观点，生成简洁自然的英文回复 | 必须先选中原推文文字 |
| 文字转写 | 需要保留原话，但不想留下大量口癖和机械重复 | 去掉无意义口癖、明显重复和确定的改口，补基础标点 | 光标位于输入框 |

### 录音时的实时字幕

开始录音后，“正在听”浮层会实时显示当前识别到的文字。左侧呼吸光表示正在录音，右侧声波会跟随真实说话音量变化。

这里显示的是低延迟临时字幕，识别过程中可能回改前面的文字。它只负责让你确认麦克风听到了什么，不会直接成为最终结果。结束录音后，OpenType 仍会把完整音频交给你选择的语音识别服务重新识别，再按照当前模式生成最终文字。

实时字幕优先使用 Apple 本机识别；如果当前语言或设备不支持，本功能可能使用 Apple 的语音识别服务。可以在“设置 → 输入体验”中关闭“录音时显示实时字幕”，关闭后不会影响正常语音输入。

## 4. 智能编辑

智能编辑把“口述整理”和“选中修改”放进同一个入口。OpenType 会在开始录音时检查是否选中了文字，并锁定本次行为，不需要手动切换子模式。

### 没有选中文字：整理口述

#### 适用情景

- 在微信里快速回复别人。
- 口述一封邮件。
- 把脑中零散的想法写进 Obsidian、Notion 或备忘录。
- 说文章初稿时自动处理重复、停顿和中途改口。
- 口述真正的清单时自动整理成列表。

#### 它会做什么

- 删除“嗯、那个、然后就是说”等无意义口头禅。
- 删除意外重复和已经放弃的半句话。
- 以后说出的修正为准，覆盖前面的错误说法。
- 把顺序混乱的口述整理成更容易阅读的顺序。
- 自动补充标点、分段和必要的列表结构。
- 根据当前应用轻微调整表达：聊天更自然，邮件更完整，笔记更有结构。
- 保留你的原意、事实、数字、名字和不确定程度。

#### 示例

你说：

> 嗯我觉得这个版本可以发了，不对，还是先给 Henry 看一下，然后明天下午，如果他没问题的话我们再发。

OpenType 写入：

> 我觉得这个版本可以先给 Henry 看一下。如果他没有问题，我们明天下午再发布。

#### 不会做什么

- 不会替你回答你口述中的问题。
- 不会凭空补充事实或论据。
- 不会把短消息强行写成正式文章。
- 不会故意把表达变得宏大、商务化或充满“AI 味”。

### 有选中文字：按指令修改

适合缩短、扩写、翻译、总结、改语气、调整格式，或删除已有文字中的重复内容。

操作方式：

1. 在当前应用里选中需要修改的文字。
2. 保持 `智能编辑` 模式，长按或双击左 `Option` 开始录音。
3. 浮层显示“已选中文字，请说修改要求”后，说出明确的编辑指令。
4. 结束录音。OpenType 会用处理后的结果替换选中文字，并把同一结果保留在剪贴板。

有效指令示例：

- “缩短一半，但不要改变原来的判断。”
- “翻译成自然英文，适合发给投资人。”
- “语气友好一点，不要显得在命令别人。”
- “整理成三个要点。”
- “保留事实，把重复的背景删掉。”

### 选区的硬规则

只要录音开始时检测到选中文字，你的语音就只会被当作修改要求，不会被当作要追加的新正文。

必须明确发出修改指令才能执行。以下情况都不会处理：

- 保持沉默。
- 只说“嗯”“那个”“然后”等语气词。
- 只补充一段普通内容，没有表达修改意图。
- 没有说清要对所选文字做什么。

这时 OpenType 会显示中性的“未执行”，原文和剪贴板保持不变，不新增历史，也不会向文字生成模型发起请求。

注意：有无选区会在开始录音时锁定。处理期间不要取消选中或切换到另一个输入框，否则替换可能失败；即使自动替换失败，成功生成的结果仍会保留在剪贴板。某些应用或网页无法向 macOS 提供选中文字，此时可以先复制到支持编辑的应用中再操作。

## 5. 中转英

### 适用情景

- 用中文回复海外同事或朋友。
- 写英文邮件、英文私信和英文产品反馈。
- 在英文社区中表达观点。
- 你知道自己想说什么，但不想先翻译再修改英文语气。

### 它会做什么

- 理解中文、英文或中英混合口述。
- 根据意图重新组织句子，而不是逐字直译。
- 去掉中文语序带来的生硬感。
- 保留你的态度、力度、语气和事实边界。
- 根据当前应用调整英文：聊天更口语，邮件更礼貌，公开社交更简洁。
- 把原话始终视为待转换的文字，而不是对 AI 发出的任务：问题会变成英文问题，请求或命令会变成英文请求或命令。

### 示例

你说：

> 我觉得这个方向是对的，但是我们可能低估了真正部署到团队里的成本。

OpenType 写入：

> I think this is the right direction, but we may be underestimating what it will actually take to roll it out across the team.

如果你说：

> 什么是 OpenCloud？

OpenType 写入：

> What is OpenCloud?

### 不会做什么

- 不会逐字硬翻。
- 不会自动增加夸奖、寒暄、口号或不存在的观点。
- 不会为了显得高级而使用生僻表达。
- 不会回答你口述中的问题，也不会执行你口述中的请求；这些内容只会被转换成英文。

## 6. Agent 模式

### 适用情景

- “帮我写一条 Twitter，表达我今天心情很好。”
- “给 Henry 写一封简短邮件，把明天的会议推迟到周五。”
- “根据这个想法，给我三个自然的小红书标题。”
- “写一段产品更新通知，语气直接一点。”
- “把下面这些要求整理成一份待办清单。”

### 它会做什么

- 把整段语音当成当前任务，而不是要原样写下的正文。
- 判断你需要的是推文、邮件、消息、标题、列表还是其他文本成品。
- 遵守你说出的语言、平台、受众、语气、长度和格式要求。
- 如果没有说清次要细节，会做最小、保守的补全，不会编造个人经历、数字或事实。
- 如果当前恰好选中了文字，会把它作为可选参考；没有选中文字也能正常使用。
- 自动读取最近完成的 Agent 任务，用来理解“继续刚才的”“沿用上次的语气”“不要重复上一条”等连续要求。
- 只返回最终成品，不会说“好的”“这是为你生成的推文”，也不会提供多套方案，除非你主动要求。

### 本地长期记忆与个人偏好

长期记忆启用后，每次正式文字任务成功完成，OpenType 都会向本机数据库追加一条可追溯记录，而不再只记录 Agent 模式：

- 原始语音转写和真正用于处理的指令。
- 当时选中的上下文与生成结果。
- 模式、应用、完成时间和记录来源。

数据保存在本机 `Application Support/OpenType/memory.sqlite3`。原始录音不会写入长期记忆，处理完成后仍会删除本地临时音频。旧版 `agent-memory.json` 与现有输入历史会在首次启动时去重迁移，不会因为升级丢失已有文字记录。

在 `设置 → 记忆与偏好 → 关于我` 可以手动填写：

- 我的职业与工作。
- 默认输出语言（仅在当前任务和模式都没有语言信号时使用）。
- 我喜欢的表达方式。
- 重要术语与正确拼写。

这些由你明确填写的内容属于高置信度资料，OpenType 永远不会自动改写。系统会完全离线地观察重复信号，每 100 条任务将常用术语、任务领域、中英文使用方式和表达偏好固化为一份独立的“已学到的偏好”。它们是低权重推断，不会冒充你亲自声明的事实。

每次生成时的优先级为：`当前明确指令 > 当前模式 > 当前应用与来源 > 关于我 > 已学到的偏好 > 默认规则`。因此，即使默认语言是中文，“中转英”也始终只能输出英文。Agent 模式会在上下文预算内额外读取最多 12 条最近 Agent 任务，以理解“继续刚才的”一类要求；其他文字模式只使用与当前任务相关的最少资料。

长期记忆可在设置中关闭。关闭后不会继续新增或读取记忆，但已有数据会留在设备上。需要从头学习时，进入 `设置 → 隐私与数据 → 本地数据管理 → 重新学习偏好` 并二次确认；此操作会移除任务事件和自动推断，但保留你亲自填写的“关于我”。设置页也可以直接在 Finder 中显示数据库文件。

术语和偏好的归纳过程不离开设备；但当某个文字任务实际调用云端模型时，与本次生成有关的少量确认资料、已学偏好和上下文会随 Prompt 发给你选择的文字服务商。

### 和智能编辑的区别

你在智能编辑且没有选中文字时说“我今天心情很好”，OpenType 会把这句话整理成自然表达。

你在 Agent 模式里说“帮我写一条 Twitter，表达我今天心情很好”，OpenType 会理解你的目标，直接创作一条可发布的推文。接着说“沿用刚才的语气，再写一条关于明天计划的”，它会参考上一项任务继续完成。

### 安全边界

任务中的“发一条推文”“发送邮件”“post this”只表示生成草稿。OpenType 不会点击发布，也不会在 Agent 模式中自动模拟回车。生成结果会写入当前输入框，同时保留在剪贴板，由你确认后决定是否发送。当前版本的 Agent 能连续完成文字任务，但还没有浏览器、邮件或其他外部工具执行权限。

## 7. X Reply

### 适用情景

- 看到一条英文推文，想快速用英文参与讨论。
- 你可以用中文说观点，但希望输出像真实英文用户写的回复。
- 需要一到三句简短、有上下文的自然回复。

### 推荐操作流程

1. 在 X/Twitter 页面选中需要回复的原推文文字。
2. 切换到 `X Reply`。
3. 长按或双击左 `Option` 开始录音。此时 OpenType 会立即读取选中的推文。
4. 有观点时，用中文、英文或中英混合说出来；不知道说什么时，可以保持安静，直接结束录音。
5. 长按模式松开左 `Option`；双击模式按任意普通键结束录音。
6. 等待浮层显示“已复制 · 在 X 回复框按 ⌘V 粘贴”。
7. 点击 X 回复框，按 `Command + V` 粘贴英文回复。
8. 由你检查后手动发布。

X Reply 始终使用剪贴板交付，不会猜测光标位置。长按和双击的输出方式完全相同。

### 它会做什么

- 把选中的原推文作为上下文。
- 有口述观点时，以你的真实观点为准；没有有效口述时，从原推文中自主寻找一个值得讨论的切入口。
- 像加入一场派对里的对话：可以补充新角度、指出张力、连接一个具体后果，或提出真正能推进讨论的问题。
- 目标是通过回复本身的价值引发讨论和主页好奇，而不是靠奉承、强行反驳、争议诱饵或明显的涨粉话术。
- 避免复述原推文、泛泛表示赞同，或把一句想法扩写成完整的小作文。
- 默认优先一句自然英文；只有你确实表达了第二层意思时才增加第二句。
- 允许像真人回复一样使用短句、不完整句和自然缩写，不强行总结或升华。
- 优先使用日常简单词和口语语法，像在评论框里直接说话，不把观点包装成高级英文写作。
- 保留必要的专业词，但会主动把 `leverage`、`underscore`、`facilitate` 一类书面词换成更普通的表达。
- 不会为了假装真人而故意添加俚语、拼写错误或刻意全小写。
- 避免“同意 + 转折 + 结论”、`Not X. Y.`、三个抽象名词并列等常见 AI 套路。
- 根据设置选择自动判断、简短直接、友好交流或有一点锋芒的风格。

### 示例

原推文：

> AI makes execution cheap, so ideas are now the only real moat.

你说：

> 我同意执行成本会下降，但组织里的信任、协调和承担责任并没有变便宜。

OpenType 可能写入：

> Execution is getting cheaper, but trust, coordination, and accountability inside an organization are not. That may be where the real moat moves next.

### 安全边界

- X Reply 永远不会替你按下发布按钮。
- X Reply 不会自动写入任何输入框。
- 即使开启“识别发送命令”，X Reply 也不会自动发送。
- 没有选中原推文时，OpenType 会直接提醒，不会让你录完后才报错。
- 自动回复不会虚构你的个人经历、私有信息、数据或原推文里没有的事实。

## 8. 文字转写

### 适用情景

- 记录访谈、会议或接近原始状态的素材。
- 需要保留说话人的原句和用词。
- 口述专有名词、数字、引用或之后准备人工编辑的素材。
- 不希望 AI 重排观点或改变表达方式。

### 它会做什么

- 把录音转换成文字。
- 删除没有语义作用的“嗯、呃、那个、就是说”等口癖。
- 删除紧挨着出现的意外重复，以及意图明确的废弃半句话。
- 遇到明确改口时保留最后的说法。
- 补充基础标点和必要的自然分段。
- 保留原来的语言、用词、观点顺序、语气、细节和不确定程度。
- 中英文夹杂时保留原本的语言切换，不把整段统一翻译成中文或英文。
- 尽量保留英文产品名、技术术语、缩写、大小写和自然空格；个人词典中的拼写优先。

### 不会做什么

- 不会重组结构。
- 不会总结、加强论点或优化表达风格。
- 不会翻译或润色。
- 不会把清晰的英文词替换成中文音译。
- 不确定某个词是否有意义时，会选择保留。

## 9. 一句话临时切换模式

不改变长期选择的模式，也可以在一次口述开头直接说明本次意图：

- `英文：我觉得这个决定应该再等一周。`
- `Agent 模式：帮我写一条简短推文，表达我今天心情很好。`
- `X Reply：我同意他的结论，但不同意他的原因。`

OpenType 会去掉开头的模式指令，只处理后面的正文。这次输入完成后，界面里原来选择的模式不会被永久修改。

X Reply 仍然需要提前选中原推文。智能编辑不需要临时模式口令：只要提前选中文字并明确说出修改要求，就会自动进入选中修改分支。

旧的 `命令输入：…` 口令继续兼容，会自动进入 Agent 模式。

## 10. 设置项说明

### 完成后自动写入当前输入框

开启：处理完成后自动粘贴到当前获得焦点的输入框。

关闭：只复制到剪贴板，由你手动粘贴。

无论这个开关是否开启，OpenType 的最终结果都会保留在剪贴板中。

### 保留本地输入历史

开启后，OpenType 会在本机保存最近 100 条正式输入记录，包括模式、结果、使用应用和时间。可以复制或重新使用。如需重置，请在设置中展开默认收起的“本地数据管理”，并完成二次确认。

应用内的首次试用不会写入历史。

### 声音反馈

开启后，OpenType Air 会用四个同一音色家族的低响度提示音区分麦克风就绪、结束录音、完成写入和出现问题。麦克风就绪音播放后即可开始说话；松开快捷键时会立即播放结束音，不需要等待云端处理完成。模式切换只显示视觉状态，不另外发声。

可在 `设置 → 输入体验 → OpenType Air → 试听` 分别试听四种状态。关闭“声音反馈”后，OpenType 不再播放任何操作音效。

### 录音时显示实时字幕

开启后，“正在听”浮层会显示临时字幕、呼吸光和随音量变化的声波。这个开关只控制实时预览，不改变最终转写和 Prompt 处理。

### 识别“发送 / press enter”命令

默认关闭。开启后，如果口述结尾是“发送”“按回车”或 `press enter`，OpenType 会在写入结果后模拟一次回车。

例如：

> 我们明天下午三点开会，发送。

OpenType 会写入“我们明天下午三点开会”，然后按下回车。

这一功能不适用于 X Reply 和 Agent 模式，避免意外公开发布。

### X Reply 风格

- 自动判断：根据原推文和你的立场选择自然语气。
- 简短直接：优先一句有力回复。
- 友好交流：更温和、更适合继续对话。
- 有一点锋芒：观点更清晰，但不会故意冒犯。

### AI 服务与 Provider Vault

OpenType 把一次语音输入拆成两个独立环节：

1. `语音识别`：把完整录音变成文字。
2. `文字生成`：按照智能编辑、中转英、Agent 模式、X Reply 或文字转写的 Prompt 处理文字。文字转写只允许最低限度清理；智能编辑检测到选区但没有明确指令时会跳过这一步。

因此可以自由组合，例如使用 ElevenLabs 转写，再用 Claude 整理；或者全部使用阿里云百炼。

| Provider | 语音识别 | 文字生成 | 默认模型 |
| --- | --- | --- | --- |
| 阿里云百炼 | 支持 | 支持 | `qwen3-asr-flash` / `qwen-plus` |
| OpenAI | 支持 | 支持 | `gpt-4o-mini-transcribe` / `gpt-5-mini` |
| ElevenLabs | 支持 | 不支持 | `scribe_v2` |
| Claude | 不支持 | 支持 | `claude-sonnet-5` |
| 豆包 · 火山方舟 | 暂未接入 | 支持 | `doubao-seed-2-0-lite-260215` |

添加 Token：

1. 打开 `设置 → AI 服务`。
2. 在 Provider Vault 展开供应商。
3. 粘贴 API Key 或 Token，点击“保存 Token”。
4. 在上方分别选择语音识别和文字生成服务。
5. 如需使用其他模型，直接修改对应的模型 ID。

保存后的 Token 不会重新显示；可以输入新 Token 覆盖，或从本机加密 Vault 移除。界面中的“已配置”只表示本机已有 Token，不代表额度、网络和模型 ID 已经通过真实请求验证。

### 转写语言

在 `设置 → AI 服务 → 转写语言` 选择语音识别的语言：

- `自动识别（支持混合语言）`：默认选项，适合中英夹杂或一段音频出现多种语言，不会锁定单一语言。
- 指定单一语言：适合整段只说中文、英语、日语、韩语、法语等一种语言，可以向语音模型提供更明确的识别提示。

OpenType 提供中文、粤语、英语、日语、韩语、德语、法语、西班牙语、意大利语、葡萄牙语、俄语、阿拉伯语、印地语、印尼语、泰语、土耳其语、越南语、乌克兰语及其他常见语言。最终识别范围仍取决于你选择的语音 Provider 和模型。

语言设置会自动转换成供应商接口：阿里使用 `asr_options.language`，OpenAI 使用 `language`，ElevenLabs 使用 `language_code`。自动识别时不会发送语言参数。

指定单一语言后，录音浮层的 Apple 实时字幕也会切换到对应语言。自动识别模式下，实时字幕仍以中文预览为主；最终结果以云端多语言识别为准。

旧版使用的 `DASHSCOPE_API_KEY` 会在首次启动时自动迁移到本机加密 Vault。豆包语音识别当前需要 App ID、Access Token、Cluster 等多项配置，因此首版只接入火山方舟的文字模型。

### 个人词典

把人名、产品名、公司名和特殊拼写加入个人词典，例如：

`Rain, OpenType, OpenClaw, Mingle, Clawborn`

词语之间可以使用逗号或换行。OpenType 会尽量保留这些拼写。

## 11. 状态提示是什么意思

- 已切换模式：模式切换成功，下一次输入使用显示的模式。
- 正在听：正在录音。
- 正在识别：正在把音频转换成文字。
- 正在整理：正在根据当前模式处理文字。
- 正在写入：正在把结果放入当前输入框。
- 完成：结果已经写入。
- 已复制：自动写入没有成功，但结果已经安全复制到剪贴板。
- 未执行：选中文字后没有收到明确修改指令，原文保持不变。
- 出现问题：下方会显示具体原因和下一步操作，例如需要选中文字、开启权限或检查网络。

## 12. 自定义 Prompt

底部的 `Prompt` 是独立的 Prompt Studio，适合在不修改代码的情况下调整每个模式的写作行为。

### 可以修改什么

可以修改以下五个可见 AI 模式：

- 智能编辑：内含“口述整理”和“选中修改”两个独立 Prompt；前者控制删减力度、结构和语气，后者控制如何执行编辑指令及保留原文。
- 中转英：控制英文的自然度、简洁程度、语气和适用场景。
- Agent 模式：控制如何理解和完成任务、如何使用已有记忆，以及不同文本成品的默认风格。
- X Reply：控制回复长度、立场表达、开头方式和社交语气。
- 文字转写：控制口癖、重复、改口和基础标点的清理边界；默认禁止重组、总结和润色。

### 怎么修改

1. 打开底部 `Prompt`。
2. 在顶部选择需要调整的模式。选择“智能编辑”时，再选择“口述整理”或“选中修改”分支。
3. 在“编辑”里直接修改文字。每次修改都会自动保存在本机，并且只影响当前模式。
4. 切换到“完整预览”，检查最终发送给文字模型的 system prompt 与 user prompt。
5. 如果效果不理想，点击“恢复默认”。

修改后的模式会显示“已自定义”，模式标签旁也会出现蓝点。点击“复制完整 Prompt”可以把当前预览复制出来做调试或留档。

### 系统会自动附加什么

为了防止自定义 Prompt 意外破坏核心行为，以下部分由 OpenType 自动管理，不在编辑框中直接修改：

- 只输出最终文字、不编造事实等全局规则。
- 当前使用的应用和对应的写作场景。
- 个人词典。
- 本次语音识别结果。
- Agent 模式的当前任务、存在时的选中参考文本，以及启用记忆后的一组最近已完成任务。
- X Reply 的原推文、你的口述观点和 X Reply 风格。
- 智能编辑选中修改分支的原文和你的语音修改要求。

也就是说，你编辑的是“这个模式应该怎么写”，OpenType 负责在每次使用时注入“这次具体要处理什么”。

## 13. 常见使用组合

### 微信和即时聊天

选择 `智能编辑`，不要选中文字，长按左 `Option` 说一两句话，松开后直接写入。

### 口述文章或长笔记

选择 `智能编辑`，不要选中文字，双击左 `Option` 后持续口述，完成时按任意普通键。

### 写英文邮件

选择 `中转英`，用中文完整说出收件人需要知道的内容。OpenType 会根据邮件场景生成完整但不过度正式的英文。

### 让 OpenType 连续完成任务

选择 `Agent 模式`，说“帮我写一条 Twitter，表达今天心情很好，英文，简短一点”。OpenType 会直接生成推文正文，写入并复制。下一次可以说“沿用刚才的语气，再写一条”，但它仍然不会替你发布。

### 回复一条英文推文

选中原推文，使用 `X Reply`，长按或双击左 Option 说出观点。回复生成并复制后，点击 X 回复框并按 `Command + V`。

### 修改已经写好的段落

选中段落，保持 `智能编辑`，明确说“缩短一点，保留结论和数字”。如果没有说出修改要求，OpenType 不会处理原文。

### 保留接近原话的口述素材

选择 `文字转写`，双击左 `Option` 连续录音。OpenType 会清理最明显的口癖和重复，但保留原来的表达顺序和语气。

## 14. 隐私与数据

- 最终录音发送到你选择的语音识别 Provider。
- 所有模式都会把识别结果发送到你选择的文字生成 Provider；文字转写也会经过最低限度清理。
- 实时字幕使用 Apple 语音识别，只作为录音预览。
- 本地临时音频会在处理完成后删除。
- API Key 只保存在 `Application Support/OpenType` 下的 AES-GCM 加密 Vault，不会写入偏好设置、输入历史或日志，保存后也不会在界面中回显。Vault 密钥与密文文件均限制为仅当前用户可读写。
- 历史记录只保存在本机，可以关闭，或在设置的二级数据管理中重置。
- Agent 任务记忆单独保存在本机，可以停止使用或经二次确认后重置；启用时，相关最近记录会随当前 Agent 任务一起发送给所选文字模型。
- OpenType 不会自动发布 X Reply。
- Agent 模式当前只完成文字任务，不会替你执行发送、发布或其他外部动作。
- 自定义 Prompt 只保存在本机的 OpenType 偏好设置中。

## 15. 使用原则

如果只记住三件事：

1. 短内容长按左 `Option`，长内容双击左 `Option`。
2. 智能编辑会自动判断有无选区；选中文字后，必须明确说出修改指令才会执行。
3. 连续文字任务使用 Agent 模式；它会记住最近任务，但所有发送和发布仍由你确认。
